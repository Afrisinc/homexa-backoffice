React + TypeScript + Vite

This template provides a minimal Project Dashboard Setup.

created by vitejs and running with pnpm(Performant Node Package Manager) package manager because it is faster, more disk-efficient, and enforces stricter dependency management, ensuring a clean and predictable frontend setup.
step 1
init project is 'pnpm create vite@latest'
and follow instructions
step 2
run project with 'pnpm dev'